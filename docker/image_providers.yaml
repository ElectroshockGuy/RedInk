# 图片生成服务配置
# 请在 Web 界面的"设置"页面中配置 API Key
active_provider: default

# 全局最大并发数（所有任务共享，防止多任务时并发数叠加超出 API 限制）
max_concurrent: 15

providers:
  default:
    api_key: ""
    base_url: https://api.openai.com/v1
    high_concurrency: false
    model: dall-e-3
    type: image_api
